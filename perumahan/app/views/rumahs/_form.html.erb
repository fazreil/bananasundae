<%= form_for(@rumah) do |f| %>
  <% if @rumah.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rumah.errors.count, "error") %> prohibited this rumah from being saved:</h2>

      <ul>
      <% @rumah.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombor %><br>
    <%= f.text_field :nombor %>
  </div>
  <div class="field">
    <%= f.label :sah %><br>
    <%= f.check_box :sah %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
